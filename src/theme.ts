import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#232429FF",
    },
    secondary: {
      main: "#dc004e",
    },
  },
});

export default theme;
